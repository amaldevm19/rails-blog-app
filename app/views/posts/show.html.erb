<div class='jumbotron mt-5'>
    <p><%= @post.body %></p>
</div>

<%= link_to 'Edit', edit_post_path(@post), :class=>'btn btn-small btn-success' %>
<%= link_to 'Delete',   post_path(@post), 
    :method => :delete, data: {confirm: 'Are you sure?'}, :class=>'btn btn-small btn-danger' %>
<hr>
<%= render 'comments/comments' %>
<%= render 'comments/newcomments' %>

